{
  "name": "sinergia-site",
  "private": true,
  "type": "module",
  "workspaces": [
    "app"
  ],
  "scripts": {
    "dev": "npm run dev --workspace app",
    "build:app": "npm run build --workspace app"
  }
}
